import { QnSpacerDirective } from './qn-spacer.directive';

describe('QnSpacerDirective', () => {
  it('should create an instance', () => {
    const directive = new QnSpacerDirective();
    expect(directive).toBeTruthy();
  });
});
